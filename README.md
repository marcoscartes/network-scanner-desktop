# Network Scanner Desktop

A standalone version of the Network Scanner (Go), providing a native desktop interface for Windows, macOS, and Linux.

## ✨ Features
- **Standalone Interface**: Runs in its own window without an address bar or browser controls.
- **Cross-Platform**: Compatible with Windows (Edge WebView2), Linux (Chrome/Chromium), and macOS (Chrome).
- **Zero Configuration**: Automatically detects your network and starts scanning.
- **Persistent Data**: Stores all discovery history and configurations in a local SQLite database.
- **Premium UI**: Dark mode, interactive charts, and real-time updates.

## 🛠️ Requirements
- **Go 1.18+**
- **Chrome, Chromium, or Edge** installed on the system (used by Lorca to render the UI).

## 🚀 How to Run
1. Clone this repository.
2. Run the application:
   ```bash
   go run cmd/desktop/main.go
   ```

## 🏗️ Building for Production
To build a standalone executable:

### Windows
```bash
go build -ldflags="-H windowsgui -s -w" -o scanner-desktop.exe cmd/desktop/main.go
```

### Linux / macOS
```bash
go build -ldflags="-s -w" -o scanner-desktop cmd/desktop/main.go
```

## 🤖 AI-Generated
This project was 100% architected and generated by an AI assistant (Antigravity).
